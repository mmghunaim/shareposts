@extends('layouts.app')

@section('content')
    {{-- <h1>{{$title}}</h1> --}}
    {{-- @if(count($services)>0)
        <ul class="list-group">
        @foreach ($services as $service )
            <li class="list-group-item">{{$service}}</li>
        @endforeach
        </ul>
    @endif --}}
    <div class="jumbotron text-center">
        <h3>This app allows you to see posts of aother people also you can join 
            to our family to get the permission to create your posts and share your thoughts</h3>
    </div>
@endsection